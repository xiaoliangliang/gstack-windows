#!/bin/bash
# Shim: delegates to compiled find-browse binary, falls back to basic discovery.
# The compiled binary handles git root detection for workspace-local installs.
DIR="$(cd "$(dirname "$0")/.." && pwd)/dist"
if test -x "$DIR/find-browse"; then
  exec "$DIR/find-browse" "$@"
elif test -x "$DIR/find-browse.exe"; then
  exec "$DIR/find-browse.exe" "$@"
fi
# Fallback: basic discovery
ROOT=$(git rev-parse --show-toplevel 2>/dev/null)
if [ -n "$ROOT" ] && test -x "$ROOT/.codex/skills/gstack/browse/dist/browse"; then
  echo "$ROOT/.codex/skills/gstack/browse/dist/browse"
elif [ -n "$ROOT" ] && test -x "$ROOT/.codex/skills/gstack/browse/dist/browse.exe"; then
  echo "$ROOT/.codex/skills/gstack/browse/dist/browse.exe"
elif [ -n "$ROOT" ] && test -x "$ROOT/.claude/skills/gstack/browse/dist/browse"; then
  echo "$ROOT/.claude/skills/gstack/browse/dist/browse"
elif [ -n "$ROOT" ] && test -x "$ROOT/.claude/skills/gstack/browse/dist/browse.exe"; then
  echo "$ROOT/.claude/skills/gstack/browse/dist/browse.exe"
elif test -x "$HOME/.codex/skills/gstack/browse/dist/browse"; then
  echo "$HOME/.codex/skills/gstack/browse/dist/browse"
elif test -x "$HOME/.codex/skills/gstack/browse/dist/browse.exe"; then
  echo "$HOME/.codex/skills/gstack/browse/dist/browse.exe"
elif test -x "$HOME/.claude/skills/gstack/browse/dist/browse"; then
  echo "$HOME/.claude/skills/gstack/browse/dist/browse"
elif test -x "$HOME/.claude/skills/gstack/browse/dist/browse.exe"; then
  echo "$HOME/.claude/skills/gstack/browse/dist/browse.exe"
else
  echo "ERROR: browse binary not found. Run: cd <skill-dir> && ./setup" >&2
  exit 1
fi
